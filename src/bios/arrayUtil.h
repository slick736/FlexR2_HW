#include <stdint.h>
#include <stddef.h>
#include <stdbool.h>

extern uint8_t arrayCompare(uint8_t *array1, uint8_t *array2, uint8_t startPosition, uint8_t compLength);
extern uint8_t mpuResultIsRaw(uint8_t *mpuResult);